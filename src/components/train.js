import React from 'react';


const Train = (props) => { //functional class


    return(
        <iframe src="https://wit.ai/" width={400} height={300}>
            <p>Your browser does not support iframes.</p>
        </iframe>
        )


};

export default Train;